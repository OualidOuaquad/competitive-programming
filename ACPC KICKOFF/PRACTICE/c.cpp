
#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define ar array


void solve(){
	int a,b;
	cin >> a >>b ;
	cout << a+b << "\n";
}

int main() {

	ios::sync_with_stdio(0);
	cin.tie(0);

	#ifndef ONLINE_JUDGE
		freopen("hello.in", "r", stdin);
		// freopen("hello.out", "w", stdout);
	#endif

	int t,a,b;
	cin >> t;

	while(t--)
	{	
		cout << "sum = ";
		solve();
	}

	return 0;
}
